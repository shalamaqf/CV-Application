export default function Header({name}) {
    return (
        <>
            <header>
                <h1>
                    {name}'s CV Application
                </h1>
            </header>
        </>
    )
}